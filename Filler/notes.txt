3 playing modes.

if ennemi closer to the center than you && center not occupied, play center mode:
	select the place that brings the most close to the center
else if ennemi not blocked, play aggressive mode:
	select the place that brings the most close to ennemie's last piece
else, play filling mode:
	select the place where the piece has a maximum of occupied neighbours
